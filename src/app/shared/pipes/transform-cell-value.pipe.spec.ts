import { TransformCellValuePipe } from './transform-cell-value.pipe';

describe('TransformCellValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformCellValuePipe();
    expect(pipe).toBeTruthy();
  });
});
