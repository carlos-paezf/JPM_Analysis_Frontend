import { NoteFieldFormDirective } from './note-field-form.directive';

describe('NoteFieldFormDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteFieldFormDirective();
    expect(directive).toBeTruthy();
  });
});
