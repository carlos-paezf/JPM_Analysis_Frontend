import { OptionalFieldDirective } from './optional-field.directive';

describe('OptionalFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionalFieldDirective();
    expect(directive).toBeTruthy();
  });
});
